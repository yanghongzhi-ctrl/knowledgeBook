# 研发待办 2026-06-04

## 本轮工程进展

- 新增前端 API 冒烟脚本：`scripts/validate_frontend_api_smoke.py`。
- 修复资源意图误判：`图形表达`、`表达方式` 等概念问题不再因包含单字 `图`、`表`、`式` 被资源导向抢答。
- 保留明确资源问法：`图5-1说明了什么`、`公式5-1怎么用`、`表5-1怎么看`、`互动脚本怎么操作` 仍进入资源导向。
- 前端资源卡预览收紧：结构化表格默认只展示短预览，长文本截断；互动脚本标题与主题相同不再重复显示。
- 前端静态资源增加版本参数：`styles.css?v=20260604a`、`app.js?v=20260604a`，减少浏览器缓存导致的旧脚本残留。
- ch04 两个图片占位资源已补绘为项目内教学示意图并完成 full release：
  - `res_ch04_011` -> `assets/ch04/images/res_ch04_011.svg`
  - `res_ch04_012` -> `assets/ch04/images/res_ch04_012.svg`
- ch04 最后 1 条 `Source_Chunks` 未核验项已完成标题边界复核：`ch04_src_064` 从 `unverified` 调整为 `partial`。
- ch05 剩余 6 条 `Source_Chunks` 已完成边界复核：全部由 `unverified` 调整为 `concept_supported`，并记录正式稿行号证据与摘要边界说明。
- 新增资源覆盖审计脚本：`scripts/audit_resource_coverage.py`，输出报告 `output/resource_coverage_audit_2026-06-04.json`。
- ch06 初始操作型知识库已接入现有发布链路：新增 `scripts/normalize_ch06.py`，绑定 `6.AutoCAD功能全景互动脚本.html`，保留操作任务、步骤、命令、错误、视频片段等专用表。
- API、发布脚本和前端冒烟脚本已扩展到 ch01-ch06。

## 当前验证基线

| 检查项 | 结果 |
|---|---:|
| 前端 API 冒烟 | 通过 |
| 自动判章标准题+学生问法矩阵 | 10815 次检查，失败 0 |
| ch01 资源评测 | 65/65 |
| ch02 资源评测 | 101/101 |
| ch03 资源评测 | 134/134 |
| ch04 资源评测 | 52/52 |
| ch05 资源评测 | 93/93 |
| ch04 full release | released，embedding 389/389，DB keyword 404/404，DB hybrid 30/30，Source_Chunks 无 unverified |
| ch05 full release | released，embedding 383/383，DB keyword 478/478，DB hybrid 30/30 |
| ch05 QA 评测 | 478/478 |
| ch06 full release | released，embedding 261/261，DB keyword 180/180，DB hybrid 30/30 |
| ch06 QA 评测 | 180/180 |
| ch06 资源评测 | 3/3 |
| 资源覆盖审计 | 已生成，ch03 仍有 3 个图片占位需人工确认；ch06 有 44 个图/视频占位待后续素材补充 |
| 前端 API 冒烟 | ch01-ch06 33 项检查，失败 0 |
| 自动判章标准题+学生问法矩阵 | ch01-ch06 15498 次检查，失败 0 |
| 浏览器桌面验证 | 通过，无横向溢出 |
| 浏览器 390px 移动端验证 | 通过，无横向溢出 |

## ch04 资源占位处理结果

| resource_id | 标题 | 当前状态 | 后续建议 |
|---|---|---|---|
| `res_ch04_011` | CDE协同交付示意 | 已补绘 SVG 教学示意图并绑定；来源标记为 `generated_teaching_diagram`。 | 后续若取得正式稿原始素材，可替换当前教学示意图。 |
| `res_ch04_012` | LOD与LOI阶段适配示意 | 已补绘 SVG 教学示意图并绑定；来源标记为 `generated_teaching_diagram`。 | 后续若取得正式稿原始素材，可替换当前教学示意图。 |

说明：这两张图是项目内补绘教学图，不是正式稿原始配图。它们用于消除资源占位并支撑学生查看类问题，后续可被正式素材无缝替换。

## ch04 Source_Chunks 复核结果

复核记录：`data/review/ch04_source_chunk_boundary_review_2026-06-04.json`。

| chunk_id | 处理结果 | 说明 |
|---|---|---|
| `ch04_src_064` | 部分确认 | 原条目为标题级证据“2.建模单位（What）”。正式稿第 85 行包含“建模单位”，第 89 行标题为“2.建模单元（What）”，第 90 行解释设计信息基本表达单元；语义一致但非逐字相同，标记为 `partial`。 |

## ch05 Source_Chunks 复核结果

原始知识包中 6 条 `word_verification=unverified` 的 `Source_Chunks` 已复核完毕。处理原则是：保留其 `knowledge_summary_not_verbatim_quote` 定位，不作为教材逐字原文引用；对正式稿支撑不足的摘要收窄边界，并在 `word_correspondence` 中记录行号证据、匹配文本和复核结论。

复核记录：`data/review/ch05_source_chunk_boundary_review_2026-06-04.json`。

| chunk_id | 处理结果 | 说明 |
|---|---|---|
| `ch05_src_025` | 收窄并确认 | 聚焦正式稿 107-109 行支持的 LDP 比例因子优化均衡，移除边界外扩展表述。 |
| `ch05_src_052` | 教学细分保留 | 正式稿 185-191 行支持缓冲区总体定义和点线面分类标题，点缓冲区场景作为教学摘要保留。 |
| `ch05_src_053` | 教学细分保留 | 正式稿 185-191 行支持缓冲区总体定义和点线面分类标题，线缓冲区场景作为教学摘要保留。 |
| `ch05_src_054` | 教学细分保留 | 正式稿 185-191 行支持缓冲区总体定义和点线面分类标题，面缓冲区场景作为教学摘要保留。 |
| `ch05_src_059` | 直接确认 | 正式稿 206-210 行直接支持多边形叠加分析的几何求交与属性融合两步逻辑。 |
| `ch05_src_085` | 收窄并确认 | 聚焦正式稿 291-299 行支持的邻接关系/距离权重定义与 Moran 指数语境。 |

## 资源覆盖审计结果

审计脚本：`scripts/audit_resource_coverage.py`。

审计报告：`output/resource_coverage_audit_2026-06-04.json`。

| 章节 | 资源状态 | 结论 |
|---|---|---|
| ch01 | 14 个资源均 `bound` | 无缺文件、无占位资源。 |
| ch02 | 23 个 `bound`，22 个 `structured` | 无缺文件、无占位资源；部分互动脚本未填 `related_kps`，当前不影响资源评测。 |
| ch03 | 27 个 `bound`，35 个 `structured`，3 个 `placeholder` | `fig_3_5` 缺正式素材；`fig_3_6`、`fig_3_14` 有同名候选资产但图面与当前标题/说明不完全一致，需人工确认后再绑定。 |
| ch04 | 19 个 `bound`，2 个 `structured` | 无占位资源；Source_Chunks 已无 `unverified`。 |
| ch05 | 30 个 `bound`，10 个 `structured` | 无占位资源；Source_Chunks 已无 `unverified`。 |
| ch06 | 1 个 `bound`，44 个 `placeholder` | 已绑定 AutoCAD 功能全景互动脚本；图像、截图和视频资源暂未制作或绑定，普通答案不主动推荐这些占位资源。 |

ch03 暂缓绑定说明：

| resource_id | 当前标题 | 候选素材 | 暂缓原因 |
|---|---|---|---|
| `fig_3_5` | 交互修改功能 | 无 | 文件路径仍指向旧占位 `chapter_03/resources/fig_3_5.png`，项目资产中未找到可确认图片。 |
| `fig_3_6` | 平面CAD系统流程图 | `assets/ch03/images/fig_3_6.emf`、`assets/ch03/images/fig_3_6.png` | 已从 EMF 转出 PNG，但图面内容是中间缓和曲线几何计算图，不是平面 CAD 系统流程图。 |
| `fig_3_14` | 纵断面CAD系统设计流程 | `assets/ch03/images/fig_3_14.png` | 图面内容更接近横断面 CAD 流程，与当前资源标题不一致。 |

## ch06 接入结果

输入材料：

- 初始知识库：`C:\Users\Michael\Desktop\knowledge\第6章_AutoCAD平台功能与使用方法_知识库_v1.0操作完备版.json`
- JSONL 初稿：`C:\Users\Michael\Downloads\第6章_答案卡_操作任务_知识点_v1.0操作完备版.jsonl`
- 建库指南：`C:\Users\Michael\Desktop\knowledge\Codex指南_第六章操作型知识库与视频分段RAG实现.docx`
- 互动脚本来源：`F:\编书\道路工程数字设计方法\05脚本及图片素材\6.AutoCAD功能全景互动脚本.html`
- PPT 参考目录：`F:\编书\道路工程数字设计方法\03 授课ppt`，不进入知识库系统。

处理结果：

| 项目 | 状态 |
|---|---|
| 原始包路径 | `data/raw/ch06/第6章_AutoCAD平台功能与使用方法_知识库_v1.0操作完备版.json` |
| JSONL 归档 | `data/raw/ch06/第6章_答案卡_操作任务_知识点_v1.0操作完备版.jsonl` |
| 互动脚本 | 已复制到 `assets/ch06/interactive_html/6.AutoCAD功能全景互动脚本.html` |
| processed 导出 | 25 个文件，包含 `operation_tasks.json`、`operation_steps.json`、`command_cards.json`、`common_errors.json`、`video_segments.json` 等操作型专用表 |
| 视频资源 | 8 个视频资源、30 个视频片段均保留为 `placeholder`，等待后续操作视频制作和时间轴标注 |
| 截图/图件 | 36 个图像资源和 150 个截图资源仍为占位，等待正式素材绑定 |
| 推荐策略 | ch06 占位资源设置 `suppress_recommendation=true`；普通答案只主动推荐已绑定互动脚本，明确问图号时可返回不可用占位状态 |

ch06 当前可用抽查：

| 问题 | 结果 |
|---|---|
| `AutoCAD如何设置AutoCAD绘图环境？` | 命中 `ans_ch06_task_001_howto`，`answer_mode=step`，推荐 `ch06_script_autocad_overview` |
| `设置AutoCAD绘图环境失败或结果不对怎么办？` | 命中 `ans_ch06_task_001_error`，`answer_mode=troubleshoot`，推荐 `ch06_script_autocad_overview` |
| `我想看第六章AutoCAD互动脚本。` | 命中 `resource:ch06_script_autocad_overview`，资源可用 |
| `我想看图6-1。` | 命中 `resource:ch06_res_fig_001`，资源标记为不可用占位，同时推荐可用互动脚本 |

## ch07-ch09 操作型章节接入结果

输入材料：
- 第7章初稿：`C:\Users\Michael\Desktop\knowledge\第7章_道路CAD软件的使用_知识库_v1.0操作完备版.json`、`C:\Users\Michael\Desktop\knowledge\第7章_答案卡_操作任务_知识点_v1.0操作完备版.jsonl`
- 第8章初稿：`C:\Users\Michael\Desktop\knowledge\第8章_道路BIM软件的使用_知识库_v1.0操作完备版.json`、`C:\Users\Michael\Desktop\knowledge\第8章_答案卡_操作任务_知识点_v1.0操作完备版.jsonl`
- 第9章初稿：`C:\Users\Michael\Desktop\knowledge\第9章_BIM_GIS集成方法_知识库_v1.0操作完备版.json`、`C:\Users\Michael\Desktop\knowledge\第9章_答案卡_操作任务_知识点_v1.0操作完备版.jsonl`
- 互动脚本来源：`F:\编书\道路工程数字设计方法\05脚本及图片素材`
- PPT 参考目录：`F:\编书\道路工程数字设计方法\03 授课ppt`，不进入知识库系统。

工程处理：
| 项目 | 结果 |
|---|---|
| 规范化脚本 | 新增 `scripts/normalize_operation_chapters.py`，统一处理第7-9章操作型知识包 |
| API/发布脚本接入 | `src/kb_rag/api.py`、`scripts/release_chapter.ps1`、`scripts/validate_frontend_api_smoke.py` 已扩展到 ch01-ch09 |
| 第7章互动脚本 | `assets/ch07/interactive_html/7.道路CAD软件使用流程.html` |
| 第8章互动脚本 | `assets/ch08/interactive_html/8.1OpenRoads Designer参数化架构.html`、`assets/ch08/interactive_html/8.2ORD软件功能总览与道路设计流程.html` |
| 第9章互动脚本 | `assets/ch09/interactive_html/9.1从模型驱动到数据驱动.html` 至 `9.6BIM_GIS在公路地质选线中的应用.html` 共 6 个 |
| 视频/截图策略 | 现阶段视频、视频片段和截图资源保留为占位，设置普通问答不主动推荐；已绑定互动脚本可被资源导向问答推荐 |

发布基线：
| 章节 | QA | 资源评测 | embedding | DB keyword | DB hybrid | 状态 |
|---|---:|---:|---:|---:|---:|---|
| ch07 | 265/265 | 2/2 | 466/466 | 265/265 | 30/30 | released |
| ch08 | 262/262 | 4/4 | 478/478 | 262/262 | 30/30 | released |
| ch09 | 256/256 | 12/12 | 384/384 | 256/256 | 30/30 | released |

运行验证：
| 检查项 | 结果 |
|---|---|
| 前端 API 冒烟 | ch01-ch09 48 项检查，失败 0 |
| 自动判章矩阵 | ch01-ch09 38043 次检查，失败 0 |
| 第7章样例 | `纬地如何新建纬地项目？` 命中 `ans_ch07_001`；`我想看道路CAD软件使用流程。` 命中 `resource:ch07_script_01` |
| 第8章样例 | `ORD参数化架构是什么？` 命中 `ans_ch08_0001`；`我想看ORD软件功能总览与道路设计流程。` 命中 `resource:ch08_script_02` |
| 第9章样例 | `数据驱动的BIM+GIS道路设计范式是什么？` 命中 `ans_ch09_001`；`我想看BIM+GIS协同设计流程。` 命中 `resource:ch09_script_03` |

注意事项：
- ch08 源稿中保留 13 组同名标准问题，发布质量报告仍作为提示记录；规范化脚本已用操作任务名或答案要点为评测题和索引补充上下文，当前不影响问答和回归。
- ch07-ch09 的操作视频尚未制作，后续补视频后应更新 `Video_Resources`、`Video_Segments`、截图文件路径、起止时间、字幕和关键帧。

## 操作型任务浏览能力

新增能力：
| 项目 | 结果 |
|---|---|
| 后端接口 | 新增 `/operation-tasks`、`/operation-task` |
| 数据范围 | ch06-ch09 的 `Operation_Tasks`、`Operation_Steps`、`Common_Errors`、关联答案卡和资源建议 |
| 前端入口 | 右侧检查器新增“本章操作任务”，支持任务列表、步骤详情、常见错误、相关问答和推荐资源 |
| 回归覆盖 | `scripts/validate_frontend_api_smoke.py` 已增加操作型接口检查 |

当前接口基线：
| 章节 | 任务 | 步骤 | 错误 | 首个任务详情 |
|---|---:|---:|---:|---|
| ch06 | 30 | 150 | 90 | 5 步，3 错误，1 个资源 |
| ch07 | 60 | 300 | 176 | 5 步，2 错误，1 个资源 |
| ch08 | 72 | 360 | 144 | 5 步，2 错误，2 个资源 |
| ch09 | 54 | 270 | 108 | 5 步，2 错误，6 个资源 |

验证结果：
- 前端/API 冒烟：ch01-ch09 60 项检查，失败 0。
- 自动判章矩阵：ch01-ch09 38043 次检查，失败 0。
- 浏览器桌面与 390px 移动宽度检查：操作任务面板无横向溢出，任务详情可展开。

## 操作型回答模板增强

新增能力：
| 项目 | 结果 |
|---|---|
| API 回答渲染 | 对 ch06-ch09 中带 `related_task` 的操作类答案卡，优先使用 `Operation_Tasks`、`Operation_Steps`、`Common_Errors` 组织主回答 |
| 操作意图优先路由 | 当问题同时命中任务名与“怎么操作/步骤/失败/错位/不显示/检查”等意图时，优先返回操作任务答案，避免被概念卡或占位视频抢答 |
| 保留概念问答 | 例如 `TIN曲面是什么？` 仍走概念答案卡，不强制转为操作任务 |
| 错误字段兼容 | `Common_Errors` 已兼容 `error_symptom`、`possible_causes`、`solution_steps`、`related_video_segment` 等字段 |

典型抽样：
| 问题 | 结果 |
|---|---|
| `创建或检查ORD项目工作环境怎么操作？` | 命中 `ans_ch08_0091`，`retrieval_order` 以 `Operation_Tasks` 开头，输出 5 步、2 个错误、2 个资源 |
| `创建TIN曲面怎么操作？` | 命中 `ans_ch09_075`，输出任务目标、前置条件、5 个步骤、2 个常见错误和互动脚本资源 |
| `为什么创建TIN曲面后结果错位/不显示/不正确？` | 命中 `ans_ch09_129`，按错误现象、可能原因、处理步骤和关联步骤组织排查 |
| `TIN曲面是什么？` | 仍命中 `ans_ch09_012` 概念解释，不触发操作型渲染 |

验证结果：
- 前端/API 冒烟：ch01-ch09 60 项检查，失败 0。
- 自动判章矩阵：ch01-ch09 38043 次检查，失败 0。
- 浏览器桌面与 390px 移动宽度检查：主回答区显示操作步骤，无横向溢出。

## 操作媒体联动基础设施

新增能力：
| 项目 | 结果 |
|---|---|
| API 媒体解析 | 操作步骤和错误记录可解析 `video_segment_id`、`screenshot_id`、视频文件路径、起止时间、状态和可用性 |
| 任务详情 | `/operation-task` 返回 `media_summary`，并在每个步骤中附带 `video_segment` 与 `screenshot` 对象 |
| 主回答 | 操作型回答会在末尾说明媒体状态；正式文件未绑定时只提示“已预留，待补充”，不误报可播放 |
| 前端显示 | 操作任务详情中新增“视频待补充/截图待补充”标签；未来文件可用后自动变为可点击媒体入口 |
| 审计脚本 | 新增 `scripts/audit_operation_media_readiness.py`，输出 `output/operation_media_readiness_2026-06-04.json` |
| 元数据补齐 | `normalize_ch06.py` 与 `normalize_operation_chapters.py` 已自动补齐步骤引用但媒体表缺失的 `Video_Resources`、`Video_Segments`、`Screenshot_Resources` 占位记录 |

媒体就绪度基线：
| 章节 | 视频资源 | 视频片段 | 可用片段 | 截图 | 可用截图 | 主要状态 |
|---|---:|---:|---:|---:|---:|---|
| ch06 | 30 | 152 | 0 | 150 | 0 | 视频/截图均为占位；步骤引用的片段与截图元数据已补齐，缺失清单为 0 |
| ch07 | 10 | 60 | 0 | 300 | 0 | 视频/截图均为占位；步骤引用的片段与截图元数据已补齐，缺失清单为 0 |
| ch08 | 17 | 84 | 0 | 360 | 0 | 视频/截图均为占位；步骤引用的片段与截图元数据已补齐，缺失清单为 0 |
| ch09 | 16 | 80 | 0 | 270 | 0 | 视频/截图均为占位；步骤引用的片段与截图元数据已补齐，缺失清单为 0 |

验证结果：
- 前端/API 冒烟：ch01-ch09 64 项检查，失败 0。
- 自动判章矩阵：ch01-ch09 38043 次检查，失败 0。
- 接口抽样：ch06、ch08、ch09 首个任务均返回 5 个视频片段位和 5 张截图位，当前可用数量为 0，符合“待补充素材”的状态。
- 浏览器检查：第8章任务详情在桌面与移动宽度均显示 5 个“视频待补充”和 5 个“截图待补充”标签，无横向溢出。

## ch06-ch09 Word教材深度核实

新增能力：
| 项目 | 结果 |
|---|---|
| Word抽取 | 新增 `scripts/audit_word_alignment_ch06_ch09.py`，直接解析教材 `.docx` 的 Word XML 正文，不依赖 `python-docx` |
| 章节范围 | 已从批注版数字化教材稿中定位 ch06-ch09 正文段落，并输出每章 `word_extract.txt` |
| 对照维度 | 同时核查 `Source_Chunks`、`Knowledge_Points`、`Answer_Cards`、`Operation_Tasks` 与 Word 正文的命中关系 |
| 补充候选 | 每章报告列出 Word 高频但知识库未直接命中的术语、章节标题和未直接命中样例，作为人工补充入口 |
| 输出文件 | 汇总报告 `output/word_alignment_ch06_ch09_2026-06-04.json`；分章报告 `output/ch06_rag_engine/reports/ch06_word_alignment.md` 等 |

本轮核实结果：
| 章节 | Word段落 | Source_Chunks | 知识点 | 操作任务 | 答案卡 | 标题覆盖 |
|---|---:|---|---:|---:|---:|---:|
| ch06 | 943 | exact 94 / partial 24 / concept_supported 4 / missing 28 | 81/81 | 30/30 | 94/180 | 45/74 |
| ch07 | 434 | concept_supported 8 / missing 142 | 141/141 | 60/60 | 265/265 | 1/17 |
| ch08 | 415 | concept_supported 14 / missing 146 | 144/144 | 72/72 | 6/262 | 1/16 |
| ch09 | 482 | exact 119 / partial 25 / concept_supported 3 / missing 3 | 74/74 | 43/54 | 2/256 | 7/24 |

处理结论：
- ch06 已清理 25 条 `Source_Chunks` 中残留的 Word XML 片段，`source_excerpt`、`summary`、`chunk_summary` 可展示字段中的 XML 残片已清零，并保留 `source_excerpt_repair_note`。
- ch07 和 ch08 的 `Source_Chunks` 多为结构化摘要而非教材原句，当前报告将其列为后续“证据边界人工复核/补充”的重点。
- ch09 正文证据贴合度较高，仅有少量任务名称与标题需要进一步核对表达差异。

## 正式媒体入库模板

新增能力：
| 项目 | 结果 |
|---|---|
| 入库模板脚本 | 新增 `scripts/build_operation_media_intake_template.py` |
| 输出文件 | `output/operation_media_intake_template_ch06_ch09_2026-06-04.csv` 与同名 JSON |
| 模板粒度 | 每个操作步骤 1 行，包含任务、步骤、视频、片段、截图、待填写时间轴、字幕、关键帧、审核人和备注字段 |
| 行数 | ch06 150 行，ch07 300 行，ch08 360 行，ch09 270 行，共 1080 行 |

## 下一步优先级

1. 基于 Word 对照报告，优先人工复核 ch07、ch08 的摘要型 `Source_Chunks`，为其补充更清晰的教材证据边界或 `word_correspondence` 说明。
2. 等第6-9章操作视频制作完成后，使用媒体入库模板补充 `Video_Resources`、`Video_Segments` 的文件路径、起止时间、字幕和关键帧。
3. 绑定 ch06-ch09 图像/截图正式素材；占位资源在绑定前继续不参与普通答案主动推荐。
4. 结合 Word 标题覆盖报告，补充必要的章节标题别名、问法和资源索引，尤其是 ch07/ch08 的实践流程标题。
5. 若后续取得 ch04 正式图件素材，替换当前两张项目补绘教学示意图。
6. 人工核对 ch03 的 `fig_3_5`、`fig_3_6`、`fig_3_14` 原始教材图题与图片，再决定是改标题绑定、补正式素材，还是继续保留占位。
7. 若修改知识包，执行发布链路并复跑本轮验证基线。
