# 研发进展记录

日期：2026-06-01  
范围：第一章样板章 RAG 精准问答  

## 已完成

- 建立第一章研发目录和原始数据目录。
- 导入第一章完整 JSON 和 jsonl 到 `data/raw/ch01`。
- 实现 Python 标准库版 `kb_rag` 工具包。
- 实现知识库校验、表拆分导出、答案卡优先检索、答案渲染、自动评测。
- 完成 PostgreSQL + pgvector schema 迁移脚本。
- 生成第一章 PostgreSQL seed SQL。
- 生成 PostgreSQL 检索 SQL：答案卡召回、证据查找、资源推荐。
- 绑定第一章 10 个 HTML 互动脚本，形成 14 条资源记录，其中 10 条为已绑定 HTML，4 条为图片占位。
- 生成 RAG trace 样例和知识库质量报告。
- 启动本地轻量 API 原型。

## 当前验证结果

自动评测：

| 指标 | 结果 |
|---|---:|
| QA 测试题 | 443 |
| Top1 命中率 | 439/443 = 99.10% |
| Top3 命中率 | 443/443 = 100.00% |
| 通过率 | 439/443 = 99.10% |

资源绑定：

| 类型 | 数量 |
|---|---:|
| 已绑定 HTML 脚本 | 10 |
| 图片占位资源 | 4 |
| 总资源记录 | 14 |

API：

```text
http://127.0.0.1:8766/health
http://127.0.0.1:8766/ask?q=为什么说BIM不是简单的三维模型？
http://127.0.0.1:8766/resources
```

## 已发现质量问题

第一章 QA 测试集中存在 4 组歧义：同一个规范化问题映射到两张不同答案卡。

| 问题 | 答案卡 |
|---|---|
| 如何理解构件化理念 | `ans_ch01_016`、`ans_ch01_065` |
| 如何理解参数化理念 | `ans_ch01_017`、`ans_ch01_068` |
| 如何理解协同化理念 | `ans_ch01_018`、`ans_ch01_071` |
| 如何理解生命周期化理念 | `ans_ch01_019`、`ans_ch01_074` |

这些问题已写入 `kb_quality_report_ch01.json`，后续建议由知识库维护端合并或明确区分问法。

## 主要输出文件

| 文件 | 作用 |
|---|---|
| `src/kb_rag/cli.py` | 命令行入口 |
| `src/kb_rag/api.py` | 本地 API 原型 |
| `src/db/migrations/001_init_pgvector.sql` | PostgreSQL + pgvector 建表 |
| `output/ch01_rag_engine/db_load/ch01_seed.sql` | 第一章数据库种子数据 |
| `data/processed/ch01/resource_bindings.json` | 第一章资源绑定 |
| `output/ch01_rag_engine/reports/eval_report_ch01.md` | 自动评测报告 |
| `output/ch01_rag_engine/reports/failed_cases_ch01.json` | 失败样例 |
| `output/ch01_rag_engine/reports/rag_trace_samples_ch01.json` | RAG trace 样例 |
| `output/ch01_rag_engine/reports/kb_quality_report_ch01.json` | 知识库质量问题 |

## 下一步

1. 安装或连接 PostgreSQL + pgvector 环境。
2. 执行 `001_init_pgvector.sql` 和 `ch01_seed.sql`。
3. 接入本地开源 embedding 模型，更新 `embedding_corpus.embedding`。
4. 将当前 Python 内存检索切换到 PostgreSQL 查询。
5. 修订 4 组歧义 QA/重复答案卡。
6. 开始第二章知识库建设模板化。

## 追加进展：embedding 与 API 增强

已继续完成：

- 接入本地 Ollama embedding API。
- 使用 `qwen2.5:0.5b` 生成第一章 186 条 `Embedding_Corpus` 向量。
- 输出向量缓存：`output/ch01_rag_engine/embeddings/ch01_embedding_vectors.jsonl`。
- 输出 pgvector 更新 SQL：`output/ch01_rag_engine/db_load/ch01_embedding_updates.sql`。
- 增加 `vector-search` 命令，用于验证本地向量召回。
- 增强本地 API，支持 `POST /ask`、CORS 预检和 trace 字段。

当前已将 embedding 模型从本地 Qwen 切换为 `bge-m3`。`bge-m3` 输出 1024 维向量，更适合正式中文语义检索。

追加验证：

| 指标 | 结果 |
|---|---:|
| 生成向量条数 | 186 |
| 向量维度 | 1024 |
| answer_card 向量 | 112 |
| knowledge_point 向量 | 74 |
| 回归 Top1 命中率 | 99.10% |
| 回归 Top3 命中率 | 100.00% |

## 追加进展：混合检索

已继续完成：

- 新增可选混合检索器 `HybridAnswerRetriever`。
- 混合策略为“关键词优先 + 向量增强 + 工程术语短语加权”。
- 默认 API 仍使用稳定的关键词/同义问法检索。
- 当请求参数 `use_vectors=true` 时启用混合检索。
- 增加 `hybrid-ask` 和 `hybrid-report` 命令。
- 输出混合检索对比报告：`output/ch01_rag_engine/reports/hybrid_retrieval_report_ch01.json`。

示例：

```powershell
Invoke-RestMethod 'http://127.0.0.1:8766/ask?q=数字孪生为什么强调虚实闭环？'
Invoke-RestMethod 'http://127.0.0.1:8766/ask?q=数字孪生为什么强调虚实闭环？&use_vectors=true'
```

对“数字孪生为什么强调虚实闭环？”这类松散问法：

| 检索模式 | 命中答案卡 | 说明 |
|---|---|---|
| keyword | `ans_ch01_012` | 相关但偏“BIM 与数字孪生比较” |
| hybrid | `ans_ch01_013` | 更贴近“虚实闭环包括哪些环节” |

本轮回归保持：

| 指标 | 结果 |
|---|---:|
| Top1 命中率 | 99.10% |
| Top3 命中率 | 100.00% |
| 通过率 | 99.10% |

## 追加进展：切换到 bge-m3

已继续完成：

- 确认 `bge-m3` 可通过 Ollama 本地安装和调用。
- 拉取 Ollama 模型：`bge-m3:latest`。
- 将默认 embedding 模型切换为 `bge-m3`。
- 重建第一章 186 条 embedding，输出 1024 维向量。
- 重建 pgvector 更新 SQL。
- 调整混合检索向量打分阈值，适配 `bge-m3` 的余弦分数区间。
- 重启本地 API，确认 `use_vectors=true` 时 trace 中显示 `model=bge-m3`。

当前输出：

| 文件 | 状态 |
|---|---|
| `output/ch01_rag_engine/embeddings/ch01_embedding_vectors.jsonl` | 已更新为 bge-m3 1024 维 |
| `output/ch01_rag_engine/db_load/ch01_embedding_updates.sql` | 已更新为 bge-m3 1024 维 |
| `output/ch01_rag_engine/reports/hybrid_retrieval_report_ch01.json` | 已刷新 |

验证示例：

```text
问题：数字孪生为什么强调虚实闭环？
混合检索命中：ans_ch01_013
答案模式：step
向量模型：bge-m3
```

回归保持：

| 指标 | 结果 |
|---|---:|
| Top1 命中率 | 99.10% |
| Top3 命中率 | 100.00% |
| 通过率 | 99.10% |

## 追加进展：PostgreSQL + pgvector 本地运行链路

已继续完成：

- 使用 Micromamba 在项目内安装 PostgreSQL 16.10 + pgvector 0.8.1。
- 初始化本地数据库目录：`D:\knowledgebase\.pgdata`。
- 启动本地数据库：`127.0.0.1:55432`，数据库名 `road_kb`。
- 执行 `001_init_pgvector.sql`，创建主表、pg_trgm 索引和 HNSW 向量索引。
- 导入第一章 seed SQL 和 bge-m3 向量更新 SQL。
- 将 `embedding_corpus.embedding` 固定为 `vector(1024)`。
- 将 `resources.related_kps` 修正为 `jsonb`，并重生成 `ch01_seed.sql`。
- 新增 `DbHybridAnswerRetriever` 和 `PgVectorSearcher`，支持数据库版向量召回。
- 新增 CLI 命令：`db-summary`、`db-vector-search`、`db-hybrid-ask`。
- API 新增 `use_db=true` 开关，启用 PostgreSQL + pgvector 混合检索。

数据库验证：

| 指标 | 结果 |
|---|---:|
| `answer_cards` | 112 |
| `knowledge_points` | 74 |
| `embedding_corpus` | 186 |
| 已写入向量 | 186 |
| answer_card 向量 | 112 |
| knowledge_point 向量 | 74 |
| 向量维度 | 1024 |

示例验证：

```powershell
py -m kb_rag.cli db-vector-search "数字孪生为什么强调虚实闭环？"
py -m kb_rag.cli db-hybrid-ask "数字孪生为什么强调虚实闭环？"
Invoke-RestMethod 'http://127.0.0.1:8767/ask?q=数字孪生为什么强调虚实闭环？&use_db=true'
```

当前示例命中：

| 检索方式 | Top1 |
|---|---|
| pgvector | `ans_ch01_013` |
| PostgreSQL 混合检索 | `ans_ch01_013` |
| API `use_db=true` | `ans_ch01_013` |

## 追加进展：评测 trace 入库

已继续完成：

- 新增 `db_eval.py`，支持将 QA 自动评测结果写入 PostgreSQL。
- 新增 CLI：`db-evaluate`、`db-eval-runs`。
- 新增数据库查询模板：`eval_run_summary.sql`、`eval_failures.sql`。
- 为 `rag_eval_cases` 增加按运行和失败状态查询的索引。
- 已完成关键词检索全量入库评测。
- 已完成 PostgreSQL + pgvector 混合检索全量入库评测。

当前数据库评测基线：

| run | 检索器 | 测试题 | Top1 | Top3 | 通过率 | 失败 |
|---|---|---:|---:|---:|---:|---:|
| `eval_ch01_keyword_20260601_194447` | keyword | 443 | 99.10% | 100.00% | 99.10% | 4 |
| `eval_ch01_db_hybrid_20260601_194550` | db-hybrid | 443 | 99.10% | 100.00% | 99.10% | 4 |

4 个失败仍为已知歧义样例：构件化、参数化、协同化、生命周期化理念在第一章知识库中各有两张可竞争答案卡。

## 追加进展：第一章歧义样例修正与 100% 基线

已继续完成：

- 定位 4 组失败样例的根因：同一泛化问法同时映射到“综合版答案卡”和“概念精简版答案卡”。
- 保留两类答案卡，不做强制合并。
- 将后半组 QA 的泛化问法改为带判别语义的问法：
  - “从几何模型与工程语义连接角度，如何理解构件化理念？”
  - “从参数控制与规则嵌入角度，如何理解参数化理念？”
  - “从多专业共享模型与变更联动角度，如何理解协同化理念？”
  - “从施工运维数据延续角度，如何理解生命周期化理念？”
- 同步更新对应 `Answer_Cards.student_question_patterns`、`Synonyms_Questions`、`RAG_Config.index_text` 和 `Embedding_Corpus.embedding_text`。
- 增量重算 4 条受影响答案卡的 bge-m3 向量，保持全量向量文件仍为 186 条、1024 维。
- 修正 SQL seed 导入策略：简单表由 `ON CONFLICT DO NOTHING` 改为可更新 upsert；答案卡冲突时也同步更新主要字段。
- 重新导入 seed SQL 和 embedding update SQL。

最新验证基线：

| 检索器 | 测试题 | Top1 | Top3 | 通过率 |
|---|---:|---:|---:|---:|
| 本地 keyword | 443 | 100.00% | 100.00% | 100.00% |
| PostgreSQL keyword | 443 | 100.00% | 100.00% | 100.00% |
| PostgreSQL + pgvector hybrid | 443 | 100.00% | 100.00% | 100.00% |

最新数据库评测 run：

```text
eval_ch01_keyword_20260601_203154_271361
eval_ch01_db_hybrid_20260601_203239_398771
```

## 追加进展：第一章内容复核增强

已继续完成：

- 抽取 Word 教材稿第一章文本，生成 `chapter1_word_extract.txt` 作为复核依据。
- 将 10 个第一章互动 HTML 脚本正式写回知识库 `Resources` 发布包。
- `Resources` 从 9 条占位记录增强为 14 条资源记录，其中 10 条 HTML 为 `bound`。
- 修正 `Source_Chunks` 分节：学习目标保留为 `ch01_sec00`，第一节演进历程证据归入 `ch01_sec01`。
- 重新生成 processed 数据、seed SQL、质量报告和 trace 样例。
- 将增强后的 seed SQL 导入 PostgreSQL。

复核增强报告：

```text
output/ch01_rag_engine/reports/ch01_content_review_enhancement.md
```

当前验证：

| 验证项 | 结果 |
|---|---:|
| JSON schema | OK |
| 本地 QA | 443/443 = 100% |
| PostgreSQL keyword QA | 443/443 = 100% |
| HTML 资源绑定 | 10 |
| 图片占位资源 | 4 |

## 追加进展：互动脚本项目内打包

已继续完成：

- 将第一章 10 个互动 HTML 脚本复制到项目目录 `assets/ch01/interactive_html/`。
- 将知识库 `Resources.file_path` 从原始教材素材绝对路径改为项目相对路径。
- 重新生成 processed 数据、资源绑定文件和 seed SQL。
- 将新版 seed SQL 导入 PostgreSQL。
- 优化资源绑定逻辑：默认按项目根目录解析相对资源路径，便于后续打包和迁移。

当前资源路径示例：

```text
assets/ch01/interactive_html/1.4BIM+GIS 图层叠加与路线适宜性分析.html
```

验证结果：

| 验证项 | 结果 |
|---|---:|
| 项目内互动 HTML 文件 | 10 |
| 相对路径存在性检查 | OK |
| PostgreSQL 资源路径 | 相对路径 |
| 本地 QA | 443/443 = 100% |
| PostgreSQL keyword QA | 443/443 = 100% |
| PostgreSQL + pgvector hybrid QA | 443/443 = 100% |

## 追加进展：AI 学习助手

已继续完成：

- 新增静态 AI 学习助手：`web/index.html`、`web/styles.css`、`web/app.js`。
- 支持切换关键词基线、本地混合、PostgreSQL + pgvector 混合三种检索模式。
- 支持查看答案、置信度、Top Hits、trace 和推荐资源。
- 推荐互动脚本可从前端直接打开项目内相对路径资源。
- 支持通过 URL 参数直接带问题打开测试页。
- 新增下一阶段研发计划文档：`docs/下一阶段研发计划.md`。

本地访问：

```text
http://127.0.0.1:5174/web/
```

已启动测试服务：

| 服务 | 地址 |
|---|---|
| RAG API | `http://127.0.0.1:8768` |
| 前端静态服务 | `http://127.0.0.1:5174/web/` |

前端验证：

| 验证项 | 结果 |
|---|---:|
| API health | OK |
| pgvector 混合提问 | 命中 `ans_ch01_013` |
| 互动资源 URL | OK |
| 桌面截图 | `output/frontend_desktop_1440x940.png` |
| 移动截图 | `output/frontend_mobile_390x844.png` |

## 追加进展：AI 学习助手复核能力增强

已继续完成：

- API 新增 `GET /answer-card?id=...`，可查看答案卡、关联知识点、证据片段、推荐资源和对应测试题。
- API 新增 `GET /eval-runs`，可读取 PostgreSQL 中最近评测 run。
- API 新增 `GET /eval-failures`，可读取指定 run 的失败样例。
- API 的 `GET /resources` 增加资源文件存在性检查和前端 URL 路径。
- 前端新增问题历史，保存在浏览器 `localStorage`。
- 前端新增答案卡详情面板，可从 Top Hits 打开。
- 前端新增评测记录与失败样例面板。
- 前端新增学习反馈面板，支持“有用 / 需修订 / 问题不清”和备注本地保存。
- 前端新增学习反馈 JSON 导出。
- 推荐资源卡片显示 `bound` / 文件缺失等状态。
- 新增一键启动脚本：`scripts/start_dev.ps1`。

新增验证：

| 验证项 | 结果 |
|---|---:|
| `/health` | OK |
| `/answer-card?id=ans_ch01_013` | OK |
| `/eval-runs?limit=2` | OK |
| `/eval-failures?limit=5` | OK，无失败样例 |
| `/resources` 文件存在性 | 10 个互动 HTML 存在，4 个图片仍为占位 |
| pgvector API 提问 | 命中 `ans_ch01_013` |
| 本地 QA | 443/443 = 100% |
| PostgreSQL keyword QA | 443/443 = 100% |
| 一键启动脚本 | OK |
| 学习反馈导出入口 | OK |

增强后截图：

```text
output/frontend_desktop_enhanced_1440x1050.png
output/frontend_mobile_enhanced_390x1000.png
```

## 追加进展：资源导向问答与前端资源预览

已继续完成：

- 后端新增资源检索评分逻辑，综合匹配资源标题、关键词、触发问法、互动脚本主题、操作步骤、图号引用和资源型意图。
- `POST /ask` 支持资源型回答模式 `resource_guidance`，当学生提问“这个时间轴脚本应该怎么看？”、“图1-1说明了什么？”等问题时，优先推荐对应互动脚本或教材图片。
- 普通知识点问答仍保留答案卡优先策略，避免资源检索干扰第一章标准 QA 基线。
- `/resources` 与 `/ask` 的推荐资源 payload 增加 `interactive_script` 元数据，前端可直接展示互动主题和操作步骤。
- 前端资源卡片新增图片缩略图、资源匹配分、脚本操作步骤展示；资源型回答不再请求答案卡详情，而是提示直接打开推荐资源。
- 已重启本地 API，当前测试入口仍为 `http://127.0.0.1:5174/web/`。

新增验证：

| 验证项 | 结果 |
|---|---:|
| 本地 keyword QA | 443/443 = 100% |
| PostgreSQL keyword QA | 443/443 = 100% |
| `这个时间轴脚本应该怎么看？` | 命中 `resource:ch01_script_timeline` |
| `图1-1说明了什么？` | 命中 `resource:ch01_fig_1_1` |
| `数字孪生为什么强调虚实闭环？` | 仍命中答案卡 `ans_ch01_013` |
| 图片资源缩略图 | OK，无坏图 |

本轮前端截图：

```text
output/frontend_resource_direct_1440x1050.png
```

说明：本轮曾启动一次全量 PostgreSQL + pgvector hybrid 评测，但运行时间超过本地工具等待窗口，已将该次未完成 run 标记为 `timeout`。最近一次完整 db-hybrid 基线仍为 `eval_ch01_db_hybrid_20260601_205705_633407`，结果为 443/443 = 100%。

## 2026-06-02 定位修订：数字化教材学习组件

根据研发定位，系统不再按教师端、学生端拆分，而是作为《道路工程数字化设计》数字化教材的一部分，统一面向学生自学和青年教师参考。

已同步调整：

- 前端名称由“AI 知识库测试台”调整为“AI 学习助手”。
- 主流程文案调整为“精准问答学习”“学习问题”“学习反馈”。
- 仍保留召回结果、答案卡详情、评测记录和检索过程，用于青年教师参考和知识库复核。
- 后续计划第五阶段改为“教材内嵌 AI 学习助手”，不再规划独立教师视图和学生视图。

## 2026-06-02 追加进展：资源导向问答评测基线

已继续完成：

- 新增 `Resource_Evaluation_Testset`，第一章共 65 条资源导向测试题。
- 覆盖 10 个互动 HTML 脚本和 4 张教材图片，重点测试“怎么看”“怎么操作”“我想看某个演示”“图 1-x 说明什么”等学习场景。
- 新增 CLI：`py -m kb_rag.cli resource-evaluate`。
- 新增评测模块：`src/kb_rag/resource_eval.py`。
- 资源检索增强：标题 token、关键词精确命中、脚本意图和图片意图分流。
- API 仲裁增强：对“我想看/想看/查看/打开”等显式资源意图优先返回 `resource_guidance`。
- PostgreSQL schema 新增 `resource_evaluation_testset` 表，seed SQL 已可导入资源评测题。

最新验证：

| 验证项 | 结果 |
|---|---:|
| 本地 QA 评测 | 443/443 = 100% |
| 资源导向问答评测 | 65/65 = 100% |
| PostgreSQL `resource_evaluation_testset` | 65 |

新增输出：

```text
data/processed/ch01/resource_evaluation_testset.json
output/ch01_rag_engine/reports/resource_eval_report_ch01.md
output/ch01_rag_engine/reports/resource_failed_cases_ch01.json
```
